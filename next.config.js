/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // enable static export
  basePath: '/truth-or-dare', // replace with your repo name
  assetPrefix: '/truth-or-dare', // same here
};

module.exports = nextConfig;